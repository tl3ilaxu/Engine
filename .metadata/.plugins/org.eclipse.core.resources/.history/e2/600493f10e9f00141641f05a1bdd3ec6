package com.tleilaxu.graphics.images;

import java.io.File;

import javax.imageio.ImageIO;

public class ImageLoader {
	public static Image load(String path){
		File file = new File(path);
		if (file.exists()){
		BufferdImage img = ImageIO.read(file);
		}
		return null;
		
	}
}
